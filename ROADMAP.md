# Roadmap

## Now — finish go-live
- [ ] Create public GitHub repo `worldcup-game`, push, enable Pages.
- [ ] Click "Run now" on the `worldcup-scores-update` task to pre-approve the
      python/git tools so future runs don't pause on permissions.

## Decide before knockouts (~early July 2026)
- [ ] Confirm penalty-shootout scoring (currently Win/Loss, not a draw).

## Optional / nice-to-have
- [ ] Add a GitHub Actions cron for true 24/7 updates (works with the app
      closed). One workflow file + API key as a repo secret.
- [ ] Touches: head-to-head history chart, per-confederation badges, a "next
      match" countdown.
